# Safe Manifests

This folder contains templates and repository-safe metadata only. Do not place corpus text, absolute private paths, credentials, or sensitive URLs here.

Completed inventories containing private path or source details should live under `${TKAF_PRIVATE_DATA_ROOT}/manifests` or `data/private/manifests` unless reviewed for commit.

