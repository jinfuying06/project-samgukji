# Database Migrations

Commit schema migrations here. Never commit a populated database, dump, backup, vector index, or raw/analysis corpus.

Migrations change application structure. Curated data imports change content. Keep them as separate, independently reversible operations.

