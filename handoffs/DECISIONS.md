# Decision Log

결정은 append-only로 기록합니다. 바뀐 결정은 기존 행을 지우지 말고 새 행에서 `supersedes`를 연결합니다.

| ID | Date | Decision | Rationale | Owner | Status | Supersedes |
| --- | --- | --- | --- | --- | --- | --- |
| D-001 | `[date]` | Artifacts are shared memory | Codex와 Claude Code 간 채팅 의존 제거 | Orchestrator | accepted | - |
| D-002 | `[date]` | Code computes; LLM explains | 수치 오류와 환각 위험 분리 | Research owner | accepted | - |
| D-003 | `[date]` | Evaluator is independent | 자기평가 편향 축소 | Product owner | accepted | - |
| D-004 | `[date]` | Product form remains undecided through S0 | 방향 변경 비용과 조기 확정 방지 | Human owner | accepted | - |
| D-005 | `[date]` | Product and data feasibility run in parallel inside S0 | 독립적인 불확실성을 빠르게 검증 | Orchestrator | accepted | - |
| D-006 | `[date]` | Stage transitions require explicit human approval | 기획·분석·개발 범위 통제 | Human owner | accepted | - |
| D-007 | `[date]` | Raw, analysis, curated app export, and runtime DB are separate private zones | 연구 재현성과 앱 serving 분리 | Data owner | accepted | - |
| D-008 | 2026-09-17 | Local `.env` created with `TKAF_PRIVATE_DATA_ROOT=../tkaf-private-data`, pointing to the existing sibling private corpus (`tkaf-private-data/raw/inbox/{history_base_zh,history_annotations_zh,romance_zh}`) | Minor, reversible, local-only config (gitignored); required to unblock read-only D0 inventory per `data/PRIVATE_DATA_SETUP.md` recommended setup | Orchestrator | accepted | - |
| D-009 | 2026-09-17 | B0: selected product concept is **Hybrid** (Explorer + Learning game combined) | Human project owner's explicit B0 answer, informed by C0 decision matrix (`product/concepts/concept_options.md`) including the owner's own personal-motivation scores (Learning game 5, Hybrid 5, Explorer 2) which outweighed the matrix's non-motivation-weighted lean toward Explorer | Human project owner | accepted | - |
| D-010 | 2026-09-17 | B0: S1 first vertical-slice sample is **적벽대전 (Battle of Red Cliffs)** | Human project owner's explicit B0 answer, matching D0's recommendation (`data/ingestion_report.md`): romance 回49–61 contiguous, history 10/65 juan, natural 3–5-person cast | Human project owner | accepted | - |
| D-011 | 2026-09-17 | B0: `HISTORY_ANNOTATION` will be sourced via **extraction from already-crawled raw**, not new external crawling — S1 Data Engineer (D1) will split the 裴松之注 commentary already embedded in `history_base_zh` `body_text` (confirmed present via marker search in 44/65 files) into a separate annotation evidence layer during normalization | Human owner chose "source it" (추가확보) for Q-006; orchestrator verified the annotation text already exists inline in the raw corpus, making a new crawl unnecessary for the 적벽대전 sample scope | Orchestrator (method), Human owner (source-it decision) | accepted | - |
| D-012 | 2026-09-17 | B0: rights status for the original 《三國志》 and 《三國演義》 texts is treated as **owner-confirmed public domain/open source** (classical-era works); this is the human owner's direct confirmation, not a documented third-party legal opinion. Any modern translation, annotation edition, or other derivative added later requires its own separate rights check. | Human project owner directly confirmed no rights issue for the original texts when answering Q-003 at B0 | Human project owner | accepted | - |
| D-013 | 2026-09-17 | S1/D1: 적벽대전 sample's `ROMANCE` range narrowed from 回49–61 (D0's original recommendation) to 回49–57 for the ingestion pipeline | Minor, reversible track-level decision by Data Engineer: 回58–61 shift to the Ma Chao/Han Sui front with 0–1 赤벽 keyword hits, so including them would dilute the vertical-slice budget without adding relevant evidence | Data Engineer (D1) | accepted | - |
| D-014 | 2026-09-17 | S1/D0-recheck: `HISTORY_ANNOTATION` (裴松之注) is delimited in the raw crawl by `〈...〉` angle brackets (U+3008/U+3009), confirmed independently by both D1's extraction pipeline (9/10 target juan balanced, 1 imbalance flagged as QC-D1-1) and R1's manual coding pass — Q-006's "source it" instruction is fulfilled by in-repo extraction, not new external crawling | Cross-validated by two independent tracks (data engineering pipeline + manual research reading) reaching the same bracket convention | Orchestrator (synthesis of D1+R1) | accepted | - |
| D-015 | 2026-09-17 | Added narrow `.gitignore` exceptions `!analysis/outputs/result.json` and `!analysis/outputs/run_manifest.json` (the blanket `analysis/outputs/*` ignore stays for every other file in that directory) | `orchestration/gates.yaml`'s B1 gate requires these two exact files as committed artifacts; A1 confirmed their content is aggregate counts + evidence_id references only, no raw/private text, so committing them does not violate `data/DATA_ZONES.md`. Future stages must still verify content-safety per file before relying on this exception — it is not a blanket "outputs are always safe" rule. | Orchestrator | accepted | - |
| D-016 | 2026-09-17 | The 적벽대전 slice **stays at 5 persons / 22 evidence spans for B1** (pipeline-validation purpose only). The human owner explicitly confirmed the real 적벽대전 episode has many more named participants already visible in the extracted data (黃蓋, 關羽, 魯肅, 趙雲, 郭嘉 all already appear as supporting figures in `research/evidence_matrix.csv`) and **an expansion pass covering more people/evidence is required after this test slice is validated, before production/B2 work locks in the current 5-person scope as final.** This must not be silently dropped once S2/S3 start. | Human project owner chose to validate the pipeline first (small N) rather than expand now, but explicitly does not want the 5-person cap to become permanent by default | Human project owner | accepted | - |
| D-017 | 2026-09-17 | Q-010 addressed via option 3 (independent second LLM coding pass + agreement statistic), per human owner's explicit choice. Result: 22/22 rows blind-recoded by a second, context-isolated agent; 20/22 exact agreement, Cohen's κ = 0.90 ("almost perfect" by convention, with the caveat that 21 mostly-singleton labels mechanically produce a low chance baseline). Both disagreements (RM-C049-P2, RM-C049-P6) were adjudicated by the orchestrator, kept as R1's original labels, and used to add two clarifying rules to `research/coding_manual.md`. **This is LLM-LLM agreement, explicitly not human validation** — the underlying human-validation gap named in Q-010 is reduced in severity (the manual is shown internally consistent) but not closed. | Human project owner's explicit choice; full method/results in `research/intercoder_reliability.md` | Orchestrator (execution), Human owner (method choice) | accepted | - |
| D-018 | 2026-09-17 | `research/evidence_matrix.csv` (S1 research working format) and `data/schemas/evidence.schema.json` (curated-app-export contract) do not match field-for-field (structured vs. free-text locator, array vs. semicolon-joined person_refs, float vs. string confidence, different excerpt/translation enums). **Not treated as a bug to force one side to match the other** — logged as a required S3 task: Architect (E0) + Data Engineer must design an explicit transform/mapping pipeline between the two before any curated export/DB import, per `product/data_to_ui_mapping.md`'s "Known schema gap" note. | U1 (UX Designer, S2) discovered this while writing real field names into `design/component_spec.md`; a research CSV and a normalized DB schema legitimately differing in shape is expected, but the missing transform step must not be discovered mid-S3-implementation | Orchestrator | accepted | - |
| D-019 | 2026-09-17 | `HISTORY_ANNOTATION` coverage for this slice's 5-person cast is **not thin** (6/22 evidence rows, same count as `HISTORY_BASE`) — `product/acceptance_criteria.md` AC-1's "정사 주석 disabled by default" assumption is revised: `SourceModeControl` defaults to 정사/정사 주석/연의 all enabled for this cast, with per-person (not per-layer) disable when that specific person has 0 rows in a layer (e.g., 諸葛亮 has 0 `HISTORY_ANNOTATION` rows and should show a disabled-with-reason state for that combination only). | U1 (UX Designer, S2) computed the real coverage number while speccing `SourceModeControl`; the S1-era AC-1 assumption predated D1's actual extraction coverage report | Orchestrator (adopting U1's finding) | accepted | - |
| D-020 | 2026-09-17 | Bug found and fixed during the D-016 expansion pass: `analysis/pipelines/compute_chibi_result.py` had the 5-person cast **hardcoded**, silently excluding supporting figures (黃蓋, 關羽, 魯肅, 趙雲, 郭嘉) from aggregation even though they already appeared as `person_refs` in the original 22 evidence rows. Fixed to dynamically discover the cast from `research/evidence_matrix.csv` at run time (`discover_cast()`), with no hardcoded person list anywhere in the pipeline. | This is exactly the class of silent-scope-lock-in bug D-016 was raised to prevent — logged as its own decision because it was a real correctness bug (undercounting), not just a scope choice | Expansion-pass agent (fix), Orchestrator (adopting) | accepted | - |
| D-021 | 2026-09-17 | D-016 (cast/evidence expansion) executed: 적벽대전 slice grew from **5→10 persons** and **22→30 evidence spans** (HISTORY_BASE 6 / HISTORY_ANNOTATION 11 / ROMANCE 13), using D1's existing 1,148-paragraph candidate pool (no new crawl). Two new coding labels added only where the existing 21 didn't fit (`alliance_persuasion`, `combat_skill_display`), each with include/exclude examples in `research/coding_manual.md`. `analysis/outputs/result.json` regenerated by actually re-running `compute_chibi_result.py` (60 metrics now, up from 43) — not hand-edited. | Human project owner's D-016 request, executed once the pipeline-validation purpose of the original 5-person slice (B1) was satisfied | Expansion-pass agent (execution), Orchestrator (adopting) | accepted | - |
| D-022 | 2026-09-17 | Three real bugs found (independently, by both backend and frontend E1 agents) and fixed by the orchestrator: (1) `scripts/validate_structure.py` scanned `node_modules` and choked on JSONC `tsconfig.json` files — fixed to skip `node_modules/.git/dist/build/coverage/__pycache__`; (2) `tests/test_project_policy.py::test_concept_is_undecided` was stale after B0 decided Hybrid (D-009) — renamed to `test_concept_decision_is_recorded_not_silent` and updated to assert the actual decided state plus a DECISIONS.md cross-reference, preserving the original guard's intent (no *silent* re-decision) rather than deleting it; (3) `data/schemas/evidence.schema.json` was missing the D-017 `coding_validation_status` field that `app/architecture/api_contract.yaml`'s `EvidenceSpan` already required — added as a required field with a matching enum, so the two contracts can't drift apart silently again. | Two independent implementation tracks flagging the same issues is a strong signal they're real, not implementer-specific confusion | Orchestrator | accepted | - |
| D-023 | 2026-09-18 | B3 (`implementation_gate`) evaluated by an independent Evaluator agent (per `agents/evaluator.md`'s independence rule — not self-scored by the orchestrator or E1). **Result: FAIL** — category-score weighted total 81.79 (which alone would be `conditional_pass` territory, ≥80), but the automatic scoring script (`evals/run_eval.py`) derives a hard blocker `coding_calibration_missing` from `double_coded_calibration_set: false`, and `orchestration/scoring.yaml`'s own rule is explicit: "총점이 높아도 hard blocker가 하나면 실패." The Evaluator's own domain judgment was that D-017's LLM-LLM-only validation does *not* trigger the gate's named `unresolved_high_risk_claim` blocker (a deliberate, disclosed, human-approved interim state) — but the generic automated script's blocker fires regardless of that judgment, and the orchestrator is not overriding the automated gate rule unilaterally. Full detail: `evals/eval_input.json`, `evals/eval_results.json`. Orchestrator additionally fixed three real documentation-staleness items the Evaluator found (`data/quality_report.json`, `analysis/analysis_plan.md` §8 Deviations, `product/acceptance_criteria.md` AC-1 — all still said "5 persons/22 rows" after the D-021 expansion to 10/30). | Independent Evaluator's automatic-check-driven result; the human owner must decide how to proceed (see `handoffs/OPEN_QUESTIONS.md`) since this is a scope/risk decision, not one the orchestrator can resolve alone | Orchestrator (process), Independent Evaluator (scoring) | accepted | - |
| D-024 | 2026-09-18 | Human project owner completed a real review of all 30 evidence rows (Q-011 option (a), via the delivered non-technical review artifact) and reported no disagreements, including on the 2 previously LLM-LLM-adjudicated rows. `coding_validation_status` flipped `llm_llm_validated_only` → `human_validated` at the single source of truth (`data/pipelines/build_curated_export.py`), re-exported, re-imported into the runtime DB, and all test literals/frontend fixtures updated to match (`app/frontend/src/components/EvidenceCard.tsx` already had a distinct, tested rendering path for this state). Full test suites re-verified green after the change (backend 46/46, frontend 33/33, typecheck/build clean). **Honest limitation, not swept under the rug:** this was a human agree/disagree audit against already-produced labels and a project paraphrase, not an independent blind re-coding from raw text — see `research/intercoder_reliability.md`'s "Human review pass" section for the exact distinction. | Human project owner's explicit choice at Q-011; full method in `research/intercoder_reliability.md` | Orchestrator (execution), Human owner (review) | accepted | - |
| D-025 | 2026-09-18 | **B3 (`implementation_gate`) explicitly overridden and approved by the human project owner**, despite two independent automated evaluations both returning `fail` (round 1: 81.79; round 2 post-D-024: 84.61 — both blocked solely by `coding_calibration_missing`, i.e. no rigorous independent-from-scratch human double-coding of `research/evidence_matrix.csv`, only LLM-LLM double-coding (κ=0.90) plus a human confirmation/audit pass (D-024)). The human owner chose Q-011 option (b): accept this as a known, disclosed limitation rather than commission a full second blind human coding pass (option (c)). **Scope of this override: continued internal development/demo use of the 적벽대전 vertical slice only.** It does not authorize public release to real end users, and does not retroactively make the coding "double-coded" in the strict sense both evaluators defined — that gap remains real and must be re-disclosed if this evidence set is ever extended toward production/public use (see conditions in `handoffs/APPROVALS.md`'s B3 row). | Human project owner's explicit choice at Q-011 (round 2) | Human project owner (decision), Orchestrator (recording) | accepted | - |
| D-026 | 2026-09-18 | Human owner requested deeper data analysis on the D-016-expanded cast (10 persons/30 evidence spans): new source-layer divergence cases for the 5 newly-added persons. A Researcher-role agent found 2 legitimate new cases — `divergence.lusu_origination_credit` (contradictory: HB-J47-P1 vs HA-J54-P4/P5, a real historiographical credit dispute over who first proposed resisting Cao Cao) and `divergence.huanggai_feigned_surrender` (complementary: HA-J54-P1 vs RM-C049-P6) — added to `analysis/pipelines/compute_chibi_result.py` and documented in `research/analysis_criteria.md`, script re-run (62 metrics, up from 60). Explicitly did NOT force a case for 趙雲 (0 history-layer coverage — his own biography juan was never ingested) or 郭嘉 (only 1 coded row total); flagged 關羽 as a candidate for a future **compatible** case needing a second pass, not added here. | Human project owner's explicit request ("1,2 둘다"); executed by a Researcher-role agent (interrupted once by a session rate limit mid-task, resumed and completed by the orchestrator) | Researcher-role agent (analysis), Orchestrator (resumption + documentation) | accepted | - |
| D-027 | 2026-09-18 | Network analysis deepened (Statistician-role agent): `analysis/pipelines/compute_chibi_result.py`'s previously-computed-but-discarded person-to-person `edges` list is now exposed in `analysis/outputs/result.json` (10 edges: HISTORY_BASE 2 / HISTORY_ANNOTATION 5 / ROMANCE 3), plus a new `network_summary` (per-layer unique node count, edge density with an explicit anti-misinterpretation caution, and a cross-layer shared-pair check — 0 pairs currently share an edge across layers, reported honestly as a real finding, not an error). No centrality metric was computed (still correctly withheld at this N, per `agents/_three_kingdoms_domain.md`). `analysis/schemas/result.schema.json` extended with `edges`/`network_summary`; a design conflict with the backend's existing metrics-kind filter was resolved by keeping these as a separate top-level field rather than forcing them into `metrics[]` and silently breaking a passing test. **Found and documented, not fixed:** `data/pipelines/build_curated_export.py` independently recomputes its own relationship edges for `/v1/events/{id}/relationships` — no single shared source of truth exists yet between that pipeline and this one; flagged in `analysis/network_analysis_plan.md` for whoever next touches either. | Human project owner's explicit request ("1,2 둘다") | Statistician-role agent (analysis), Orchestrator (verification) | accepted | - |
| D-028 | 2026-09-18 | Evidence coding expanded again ("더많은원문코딩", human owner's explicit request): 30→**48 rows**, same 10 persons (no persons forced in — juan 10/31/48/56 reviewed and judged not genuinely relevant, not padded). Newly coded: `HISTORY_BASE`/`HISTORY_ANNOTATION` juan 55 (selectively, cast-relevant rows only), `ROMANCE` 回51/53/54/56/57. 1 new coding label (`reciprocal_combat_mercy`). **2 more divergence cases found** (total now 7): `divergence.huanggai_wound` (complementary — closes what was previously misdescribed as a pure coverage gap) and `divergence.zhouyu_lusu_succession` (compatible — first same-claim agreement case in this slice). 關羽 re-checked, still no qualifying cross-layer pair (not forced). Script re-run: 65 metrics, 20 edges. **Bug found and fixed:** the curated-export transform was about to apply `coding_validation_status: human_validated` globally to all 48 rows, when only the original 30 have actually been human-reviewed (D-024) — fixed to a per-row rule keyed on `extraction_reviewer`, with a conservative dataset-level rollup (`human_validated` only if every row is). Orchestrator independently verified via direct SQLite query: active DB version = 30 `human_validated` + 18 `llm_llm_validated_only`, dataset-level correctly `llm_llm_validated_only`; prior versions preserved, not deleted. Orchestrator additionally cleaned up `research/analysis_criteria.md` (removed 2 orphaned/duplicate table rows left over from sequential agent edits, updated stale "5 persons/22 rows" references to 10/48 throughout). | Human project owner's explicit request ("더많은원문코딩") | Researcher-role agent (coding + bug fix), Orchestrator (independent DB verification + doc cleanup) | accepted | - |
| D-029 | 2026-09-18 | **Human owner's risk-tolerance stance on coding validation, stated directly:** "내가 보니까 굳이 확인 안해도 될거같더라. 너네가 나보다 작업 잘함" — the human owner explicitly declines to be a routine manual reviewer/bottleneck for future evidence-coding additions (e.g., the 18 rows added in D-028), trusting the existing LLM-LLM double-coding process (κ agreement) as sufficient for personal/internal-demo use. **This does not remove or retroactively satisfy the D-017/D-025 disclosure requirement** — `coding_validation_status` must keep distinguishing `human_validated` from `llm_llm_validated_only` per row exactly as it does now, and this disclosure must still be re-examined before any real public release (unchanged boundary from D-025). It only means the orchestrator should not treat "get the human to manually spot-check new rows" as a required step going forward for this project's current internal scope, unless the human asks again. | Human project owner's direct statement | Human project owner (decision), Orchestrator (recording) | accepted | - |
| D-030 | 2026-09-18 | **Product vision substantially expanded**, resolving Q-005/Q-009 and opening Q-012: (1) learning-game mode must teach data-analysis concepts from zero ("초등학생도 이해"), explaining the full evidence→code→weight→score path and how 정사-weighting vs 연의-weighting changes a person's derived profile; (2) two named target personas — data-analysts drawn to the 삼국지 dataset itself (Explorer-first) and casual 삼국지 fans/data-analysis beginners (Learning-game-first) — map onto the existing Explorer/Learning-game split, no new mode structurally required; (3) a "what-if" counterfactual scenario feature is wanted (e.g. 제갈량→위나라), eventually rendered as an 8-bit game — orchestrator proposed a two-part framing (real weight-sensitivity exploration + a separately-labeled fictional `HYPOTHETICAL` scenario layer, see Q-012) that still needs the human owner's explicit sign-off before implementation; (4) the 8-bit visual/character asset production itself is **explicitly deferred** — human owner named it as needing a separate specialized (art/asset-generation) effort, not to be started now, "이해만" (understanding/planning only) needed at this time. | Human project owner's explicit product-vision statement | Human project owner (vision), Orchestrator (synthesis + Q-012 framing proposal) | accepted | - |
| D-031 | 2026-09-18 | Q-012 resolved: human owner chose **option (a) only** for the "what-if" feature — a real, evidence-grounded weight-sensitivity exploration (정사-weighted vs 연의-weighted, user-adjustable), explicitly rejecting option (b)'s fictional counterfactual-narrative layer. No new `HYPOTHETICAL` source-layer-like concept is introduced. This means "what-if" in this project means "what if we trust 정사 more than 연의 (or vice versa)," not "what if history had gone differently" — a real, data-grounded question, not a fabricated one. | Human project owner's explicit choice ("whatif시뮬레이션은 A가 맞아") | Human project owner (decision) | accepted | - |
| D-032 | 2026-09-18 | Q-012 option (a) implemented and independently verified end-to-end: `ContributionBreakdown.tsx` real weight-lab (3 independent sliders, `zero_coverage_warning`/`empty`/`loading`/`error`/`default` states), `ScoreBreakdown.tsx`'s weight-lab button enabled, elementary-level Korean intro text added. First implementation attempt (a fork) returned a "launched in background" non-answer with 0 tool calls and no file changes — caught by the orchestrator checking `git status` before trusting the report, then re-dispatched with an explicit "do the work yourself, do not delegate further" instruction; the retry did the real work (34 tool calls). Orchestrator independently re-verified beyond trusting the retry's own report: re-ran `npm test` (46/46) and `tsc --noEmit` directly, then drove the live app with Playwright against the real backend (not mocks) — confirmed 曹操 at 연의=100%/others=0% shows exactly the 5 labels documented in `analysis/scoring_model.md`'s worked example, and 趙雲 at 정사=100% correctly shows the "근거가 없어요" zero-coverage banner rather than an empty/zero breakdown. No console errors. | Human project owner's explicit request ("작업 진행해") | Frontend-role agent (implementation, after one failed attempt), Orchestrator (catch + re-dispatch + independent live verification) | accepted | - |
