# Open Questions

| ID | Question | Why it matters | Options | Needed by | Owner | Status |
| --- | --- | --- | --- | --- | --- | --- |
| Q-001 | Which product concept should B0 select? | 사용자·루프·개발 범위를 결정 | **resolved: Hybrid** — human project owner's direct B0 answer, recorded in `product/concepts/concept_decision.yaml` and `handoffs/DECISIONS.md#D-009` | B0 | Human owner | resolved |
| Q-002 | Where is the private raw corpus and may agents read it? | D0 inventory 착수 | resolved: sibling dir `../tkaf-private-data` (`.env` sets `TKAF_PRIVATE_DATA_ROOT`), read-only confirmed, see D-008 | S0 | Human owner | resolved |
| Q-003 | Which crawled sources/editions and rights apply? | 분석·앱 공개 가능 범위 | **resolved (owner-confirmed, not a written legal opinion):** human project owner confirmed both `history_base_zh` (陳壽《三國志》) and `romance_zh` (羅貫中《三國演義》) original texts are classical-era public-domain/open-source works with no rights issue. See `handoffs/DECISIONS.md#D-012`. Any modern translation or third-party derivative added later still needs its own rights check. | B0 | Data/rights owner | resolved |
| Q-004 | Which small corpus sample should S1 normalize? | 비용·검증 범위 | **resolved: 적벽대전 (Battle of Red Cliffs)** — human project owner's direct B0 answer, matches D0's recommendation. See `handoffs/DECISIONS.md#D-010`. | B0 | Human owner | resolved |
| Q-005 | Hybrid includes a learning-game mode — what is its primary learning level? | 커리큘럼과 UI 난이도 (S1 P1 product planning 입력) | **resolved (2026-09-18, D-029):** beginner, explicitly "elementary-school" level. Must teach data-analysis concepts from zero (what is an evidence span, what is a source layer, why does the same person score differently depending on weighting) — not assume any prior data-literacy. This is the default for the learning-game mode specifically; Explorer mode may still serve more literate users without re-explaining basics every time (see Q-009). | S1/P1 | Human owner | resolved |
| Q-006 | `HISTORY_ANNOTATION` (裴松之注 등) layer has 0 files as a separate folder — proceed to S1 without it, or source it first? | Hybrid 개념의 "정사-주석 충돌 병기" 핵심 비교 기능과 R1 evidence matrix 범위에 직접 영향 (D0 B-D0-1, R0 Q-R0-1) | **fully resolved.** D1 extracted 432 `HISTORY_ANNOTATION` paragraphs (vs. 479 `HISTORY_BASE`, 237 `ROMANCE`) via a `〈...〉` bracket-delimiter pipeline, independently cross-validated by R1's manual coding pass using the same bracket convention (D-014). Coverage is thick, not thin — no re-open needed. R1's `evidence_matrix.csv` includes 6 real `HISTORY_ANNOTATION` spans. | S1/D1 | Data engineer | resolved |
| Q-007 | Personal motivation score (decision-matrix weight 5) for each concept | 프로젝트 오너의 주관적 동기이며 PM 에이전트가 대신 추정 불가 (`product/concepts/concept_options.md`) | **resolved:** human owner scored Learning game and Hybrid high (5), Explorer lower (2). Recorded in `product/concepts/concept_options.md`. | B0 | Human owner (project owner) | resolved |
| Q-008 | Is a minimal user-validation signal (informal interview/observation) needed before B0, or does B0 proceed on the PROJECT_BRIEF hypothesis alone? | `product/problem_definition.md`/`target_user.md`의 "Current workaround"·"Evidence the problem exists"가 근거 없이 TBD로 남아 있음 — 추측으로 채우지 않음 | proceed on hypothesis / gather minimal validation first — not addressed by the B0 answer; treated as proceeding on hypothesis by default unless the owner says otherwise | S1 | Human owner | open |
| Q-009 | What is the primary user's assumed data-literacy level? | Hybrid의 explorer 모드와 learning-game 모드 각각의 UI 난이도 가정에 필요 (`target_user.md`) | **resolved (2026-09-18, D-029): mixed, explicitly two named personas.** (1) "삼국지 데이터 분석 자체가 흥미로운 데이터 분석가" — comfortable starting in Explorer mode without hand-holding. (2) "그냥 삼국지를 즐기고 싶은 사람" / data-analysis complete beginner — needs the learning-game mode's from-zero explanation (Q-005). Default/entry experience should assume persona (2) (more explanatory), with Explorer available for persona (1) to skip ahead — matches the existing Explorer/Learning-game split, no new mode needed. | S1/P1 | Human owner | resolved |
| Q-010 | No inter-coder reliability check exists for `research/coding_manual.md`/`evidence_matrix.csv` (적벽대전 slice), **and the single existing coding pass was performed by an LLM agent (R1), not a human researcher** | `agents/_three_kingdoms_domain.md` requires ≥2 independent coders with a reported agreement statistic; `orchestration/gates.yaml`'s `research_product_alignment_gate` (B1) lists `unvalidated_llm_autocoding_used_as_ground_truth` as a **hard blocker** | **partially resolved via option (c), human owner's explicit choice (D-017):** a second, context-isolated LLM coder blind-recoded all 22 rows — 90.9% exact agreement, Cohen's κ = 0.90 (full detail in `research/intercoder_reliability.md`). This shows the coding manual is applied consistently by independent readers, but **both readers are still LLM agents — this is not human validation.** Remaining decision: is LLM-LLM agreement sufficient to proceed past B1 for this internal-demo stage, with human review still required before B2/production treats this evidence as ground truth? | B1 (method resolved) / B2 (human validation still required before production) | Human owner | open — narrower scope: confirm LLM-LLM check is sufficient for B1, human validation still required before B2 |
| Q-011 | **B3 (implementation_gate) evaluation returned FAIL** (D-023): weighted score 81.79 (`conditional_pass` territory alone), but `evals/run_eval.py` derives a hard blocker `coding_calibration_missing` from `double_coded_calibration_set: false` — i.e., this is the same underlying gap as Q-010/D-017 (no human double-coding), now hit as a formal automated gate failure at B3 rather than just a disclosed risk. How should the project proceed? | `orchestration/gates.yaml`: a hard blocker fails the gate regardless of total score ("총점이 높아도 hard blocker가 하나면 실패") — the orchestrator cannot override this rule unilaterally; only the human owner can decide the path forward | **resolved: option (b) — explicit override** (2026-09-18, D-025). After option (a) (D-024) improved the score (81.79→84.61) but did not clear the gate, the human owner reviewed both rounds' full results and chose to accept the residual gap (no from-scratch independent human double-coding) as a known, disclosed limitation rather than commission option (c). Recorded in `handoffs/APPROVALS.md`'s B3 row as `approved_with_conditions`, scoped explicitly to continued internal development/demo work — **not** to public release. | B3 | Human owner | resolved |
| Q-012 | Human owner wants a "what-if" simulation (e.g., "제갈량이 적벽대전 때 위나라로 갔다면?") presented eventually as an 8-bit game. This is counterfactual — no real evidence describes a timeline that didn't happen, so it can't be computed the way `divergence`/`coverage` are. How should this be framed so it doesn't violate the project's no-fabricated-history rule? | `agents/_three_kingdoms_domain.md`/`README.md` explicitly forbid dressing up fabricated content as historical fact; a naive "simulate alternate history" LLM feature would violate this directly | **resolved (2026-09-18, D-031): option (a) only** — a real, evidence-grounded weight-sensitivity "what-if" (정사-weighted vs 연의-weighted, user-adjustable), no fictional counterfactual-narrative layer. The `HYPOTHETICAL` concept proposed as option (b) is dropped, not built. | Next scope decision | Human owner | resolved |
